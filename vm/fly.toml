app = "leg-tech-vms"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "off"
  auto_start_machines = true

[[vm]]
  memory = "2gb"
  cpu_kind = "shared"
  cpus = 2
